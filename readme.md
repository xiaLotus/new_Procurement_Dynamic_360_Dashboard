# 修正檔案


針對分批與合併(v1)，發驗收mail矯正資訊，2025/10/20

Planned_Purchase_Request_List
✅ 修改 1：saveFiltersToJSON (位置：~1049 行)
   改動：改進合併邏輯
   重點：先複製主頁面欄位，再保留 ert_ 欄位
```

---

### 📄 eRT.js
```
✅ 修改 1：data() 區塊 (位置：~3-145 行)
   改動：加上兩個新變數
   新增：
   - _isLoadingFilters: false
   - _saveTimeout: null

✅ 修改 2：debounceSaveERTFilters (位置：methods 區塊，新增)
   改動：新增方法
   用途：500ms 防抖保存

✅ 修改 3：saveERTFilters (位置：~5748 行)
   改動：先清除舊 ert_ 欄位，再加入新欄位
   重點：避免累積重複

✅ 修改 4：loadERTFilters (位置：~5804 行)
   改動：加上 _isLoadingFilters 標記
   重點：防止載入時觸發 watch 保存

✅ 修改 5：mounted() (位置：~6365 行)
   改動：調整 loadERTFilters() 位置
   重點：移到 fetchData() 之前

✅ 修改 6：watch 區塊 (位置：~6390 行)
   改動：所有 checkedXXX 監聽器加上標記檢查
   重點：
   - 加上 if (this._isLoadingFilters) return;
   - 改為調用 this.debounceSaveERTFilters();